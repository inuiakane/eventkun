<h1>開催されるイベント</h1>
<br>
 <%= link_to '新しいイベントを作成する', new_event_path, class: "btn btn-primary" %>
<br><br>
<!--table class="table table-bordered table-hover">
  <thead>
    <tr>
      <th>イベント名</th>
      <th>イベント内容</th>
      <th>開始日時</th>
      <th>終了日時</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= event.name %></td>
        <td><%= event.info %></td>
        <td><%= event.start_at %></td>
        <td><%= event.end_at %></td>
        <td><%= link_to 'Show', "/events/#{event.key}" %></td>
        <td><%= link_to 'Edit', edit_event_path(event) %></td>
        <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table-->

<div class="row">
 <% @events.each do |event| %>

      <div class="col-md-4"> 
      <div class="panel panel-info">

        <!--div class="bg-info"--> 
        <div class="panel-heading">
        <h5 class="panel-title"><%= event.name %> </h5>
          </div>
          <div class="panel-body">
        <%= event.info %> <br>
        <%= event.start_at %> <br>
        <%#= event.end_at %> <br>
            </div>
        <div class="panel-footer">
        <%= link_to 'Show', "/events/#{event.key}" %>
        <%= link_to 'Edit', edit_event_path(event) %>
        <%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } %></div>
          <!--br><br-->
          </div>
       </div>
   <% end %>
  </div> 
</div> 


